import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cookies",
  description: "A page that explains how to work with cookies",
};

const Page = () => {
  return (
    <section>
      <p>section that explains how to use cookies</p>
    </section>
  );
};

export default Page;
